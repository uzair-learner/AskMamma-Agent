# Agents

Purpose: hold the multi-agent layer used by the AskMamma learning project.

Responsibilities: define each agent's prompt, responsibilities, tools, routing rules, logging guidance, and trace metadata.

Interactions: the LangGraph workflow imports these modules, the API exposes their behavior, and the memory layer stores their decisions.
