# MCP

Purpose: demonstrate a simple MCP-compatible server shape.

Responsibilities: tool, resource, and prompt registries plus JSON-RPC handling.

Interactions: exposed under `/mcp/*` endpoints by the API layer.
