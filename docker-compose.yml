services:
  backend:
    image: python:3.13-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=/app/src
    command: sh -c "pip install -r requirements.txt && python scripts/seed_data.py && uvicorn inventory_pilot_ai.main:app --host 0.0.0.0 --port 8000"
  frontend:
    image: python:3.13-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8501:8501"
    depends_on:
      - backend
    environment:
      - PYTHONPATH=/app/src
    command: sh -c "pip install -r requirements.txt && streamlit run src/inventory_pilot_ai/ui/app.py --server.address 0.0.0.0 --server.port 8501"
