# API

Purpose: expose the project through FastAPI.

Responsibilities: agent endpoints, inventory endpoints, document routes, reports, and system metadata.

Interactions: acts as the entrypoint for UI, MCP, A2A, and tests.
