# RAG

Purpose: store retrieval-augmented generation building blocks.

Responsibilities: chunking, embeddings, vector storage, and retrieval.

Interactions: powers `DocumentAgent` and document API endpoints.
